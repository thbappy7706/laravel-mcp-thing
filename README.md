# Laravel MCP – Build Your First MCP Server

Building the first **Model Context Protocol (MCP)** server using Laravel.

## 📦 Installation

```bash
composer require laravel/mcp
php artisan vendor:publish --tag=mcp-config



